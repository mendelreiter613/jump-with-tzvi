
export interface JumpStory {
  title: string;
  biography: string;
  jumpingStyle: string;
  funFacts: string[];
}

export interface JumpQuote {
  quote: string;
  altitude: string;
}

export interface GeneratedImage {
  url: string;
  prompt: string;
}
